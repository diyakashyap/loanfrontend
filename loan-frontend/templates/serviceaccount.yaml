{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "loan-frontend.serviceAccountName" . }}
  labels:
    app.kubernetes.io/name: {{ include "loan-frontend.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
{{- end }}
